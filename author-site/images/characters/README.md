# Character portraits (optional)

Nothing references this folder by default &mdash; the character lists on
book and world pages are text-only. If you would like to add portraits,
a square-ish image around 500x500px works well. Add an <img> tag to the
relevant page following the same pattern used for cover images.
