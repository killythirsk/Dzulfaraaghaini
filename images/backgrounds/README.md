# Background images (optional)

Nothing references this folder by default &mdash; the hero sections use a
plain paper-colored background defined in css/style.css. To add a wide
background image (around 1600x900px, compressed for the web), add a
background-image line to the .hero rule in css/style.css pointing at a
file here.
